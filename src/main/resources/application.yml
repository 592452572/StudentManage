spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://localhost:3306/student_manage?useUnicode=true&characterEncoding=utf8&characterSetResults=utf8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
mybatis:
  # ÷ÿ“™≈‰÷√
  type-aliases-package: com.white.model
  mapper-locations: classpath:mapper/*.xml